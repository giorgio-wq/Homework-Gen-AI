/**
 * In-page anchors that other pages link to. Kept in one place so a link's
 * `hash` and the element's `id` cannot drift apart.
 */

/** About page: the partners section, opening on the intro with all portraits. */
export const PARTNERS_ANCHOR = "team";
